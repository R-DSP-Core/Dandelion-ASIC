# ASIC Node Tests

This directory contains deterministic tests for descriptor fields, Tensor
arithmetic, Spatial decisions, cycle counts, and SU ready/valid backpressure.
The implemented cases and expected values are documented in
`../../docs/test_nodes.md`.
