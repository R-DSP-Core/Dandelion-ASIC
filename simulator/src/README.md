# Cycle-Model Sources

This directory implements the common protocol and the two C++20 cycle models.
Only C++ sources and this directory README are kept here. Matching interface
descriptions are `../../docs/protocol.md`, `../../docs/tensor_node.md`, and
`../../docs/spatial_node.md`.
