# C++20 Simulator

This directory contains the executable reference models for the Dandelion-ASIC
nodes. Public interfaces are under `include/`, implementations under `src/`, and
small deterministic checks under `tests/`.
