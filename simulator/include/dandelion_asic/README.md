# Dandelion ASIC C++ Interfaces

- `protocol.hpp` defines descriptors, completion records, ready/valid wires,
  the scratchpad, and the common execution-node interface.
- `tensor_node.hpp` declares the Tensor cycle model.
- `spatial_node.hpp` declares the Spatial cycle model.

Behavior and arithmetic widths are specified in `../../../docs/`.
